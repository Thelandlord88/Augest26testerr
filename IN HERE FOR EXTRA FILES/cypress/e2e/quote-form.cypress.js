// Placeholder to avoid empty test suite failure when Jest picks up this file.
// Cypress tests are executed separately via Cypress, but Jest scans __tests__.
describe('Cypress placeholder', () => {
	it('placeholder passes', () => {
		expect(true).toBe(true);
	});
});
