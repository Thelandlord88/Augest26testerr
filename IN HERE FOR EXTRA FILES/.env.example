# Edge middleware toggle (default off for dev & CI)
USE_EDGE=false
# Example environment variables
# Copy to .env and fill in your own values. Do not commit .env.

# OpenAI API key used by AI scripts under scripts/
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional runtime overrides
# BASE_URL=
# SITE_ORIGIN=
